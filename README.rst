================================
The zodb.py3migrate distribution
================================

Helper to check if ZODB is Python 3 ready by displaying binary fields that
might need conversion to unicode.

This package is compatible with Python version 2.7.
